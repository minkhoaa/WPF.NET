<UserControl x:Class="MyStore.WpfApp.Views.CategoriesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <DockPanel Margin="10">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,8">
            <Button Content="Add" Width="80" Margin="0,0,8,0" Command="{Binding AddCommand}" />
            <Button Content="Edit" Width="80" Margin="0,0,8,0" Command="{Binding EditCommand}" />
            <Button Content="Delete" Width="80" Margin="0,0,8,0" Command="{Binding DeleteCommand}" />
            <Button Content="Refresh" Width="80" Command="{Binding RefreshCommand}" />
        </StackPanel>
        <DataGrid ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding CategoryId}" Width="Auto"/>
                <DataGridTextColumn Header="Name" Binding="{Binding CategoryName}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
    
</UserControl>
